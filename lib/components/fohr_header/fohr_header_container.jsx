import { connect } from 'react-redux';
import FohrHeader from './fohr_header'

const mapStateToProps = ({}) => ({

})

const mapDispatchToProps = (dispatch) => ({

})

export default connect(
    mapDispatchToProps,
    mapDispatchToProps
)(FohrHeader);
